name: Test

on: [push]

jobs:
  test-build:
    runs-on: ubuntu-lastest

    steps:
      - name: ls # Instalar paquetes
        run: | # directiva pipe que permite generar string multilinea
          echo "Hola mundo" >> hola.txt
          cat hola.txt